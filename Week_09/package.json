{
  "name": "week_09",
  "version": "1.0.0",
  "description": "前面已经实现了，拿到一个 url 发送 `request` 请求，并拿到 `response` 结果。这部分的目的是：",
  "main": "client.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "css": "^3.0.0"
  }
}
